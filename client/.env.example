# Copy this file to ".env.local" for local dev — Vite reads VITE_* variables automatically.
# In production, set this as an Environment Variable in your Vercel project settings.

# URL of your deployed backend (no trailing slash).
# Leave unset in local dev — localhost:5000 is the default.
VITE_API_URL=https://your-backend.onrender.com
